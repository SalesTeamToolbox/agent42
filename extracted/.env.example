# ── Model API Keys ─────────────────────────────────────────────────────────────
# Get your NVIDIA key at: https://build.nvidia.com (free tier available)
NVIDIA_API_KEY=nvapi-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Get your Groq key at: https://console.groq.com (free tier available)
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── Dashboard Auth ──────────────────────────────────────────────────────────────
DASHBOARD_USERNAME=admin

# Option A: Plaintext password (initial setup only)
DASHBOARD_PASSWORD=changeme-right-now

# Option B: Bcrypt hash (recommended — generate with: python -c "from passlib.context import CryptContext; print(CryptContext(['bcrypt']).hash('yourpassword'))")
# DASHBOARD_PASSWORD_HASH=$2b$12$xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# JWT secret — generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=change-me-to-a-long-random-string-at-least-32-chars

# ── Orchestrator Config ─────────────────────────────────────────────────────────
# Default repo path (git repo to create worktrees from)
DEFAULT_REPO_PATH=/home/youruser/projects/myproject

# Max agents running simultaneously (recommended: 2-3 for 6GB VPS)
MAX_CONCURRENT_AGENTS=3

# Path to tasks.json queue file
TASKS_JSON_PATH=tasks.json
