---
phase: 50-strip-harness-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/frontend/dist/app.js
autonomous: true
requirements:
  - STRIP-01
  - STRIP-02
  - STRIP-03
  - STRIP-04
  - STRIP-05
  - STRIP-06
  - STRIP-07
  - STRIP-10
  - STRIP-12
  - CLEAN-02

must_haves:
  truths:
    - "No harness render functions exist in app.js"
    - "Sidebar shows only kept pages: apps, tools, skills, reports, settings"
    - "Renderers map contains only kept page entries"
    - "Default page is apps, not tasks"
    - "No harness state fields pollute the state object"
  artifacts:
    - path: "dashboard/frontend/dist/app.js"
      provides: "Stripped frontend SPA with only intelligence layer pages"
      contains: "renderApps"
  key_links:
    - from: "dashboard/frontend/dist/app.js"
      to: "dashboard/server.py"
      via: "fetch calls to /api/* endpoints"
      pattern: "fetch.*api/(apps|tools|skills|reports|settings|memory|effectiveness|providers)"
---

<objective>
Strip all harness page render functions, load functions, state fields, sidebar entries, and renderer map entries from the hand-written frontend SPA.

Purpose: Remove ~3,000+ lines of dead frontend code for harness features (Mission Control, Workspace/IDE, Agents, Teams, Approvals, Chat, Status, GitHub, Profiles/Persona). The remaining app.js serves only the intelligence layer admin pages.

Output: A lean app.js with only kept pages: Apps, Tools, Skills, Reports, Settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frood-dashboard/ROADMAP.md
@.planning/workstreams/frood-dashboard/REQUIREMENTS.md
@.planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-CONTEXT.md
@.planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove harness render functions and load functions from app.js</name>
  <files>dashboard/frontend/dist/app.js</files>
  <read_first>
    - dashboard/frontend/dist/app.js (full file — understand structure before cutting)
    - .planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-RESEARCH.md (render function line map at lines 196-219, load functions at lines 186-195)
  </read_first>
  <action>
Delete all harness render functions from app.js. Per D-27, this is a hand-written SPA — surgical line deletion, no build system.

**Render functions to delete** (verify line numbers by grepping `function renderX` before cutting):

| Function | Approx Start Line | Approx Size |
|----------|-------------------|-------------|
| renderStats | L1821 | ~20 lines |
| renderTasks | L1842 | ~40 lines |
| renderKanbanBoard | L1881 | ~62 lines |
| renderListView | L1944 | ~42 lines |
| renderActivitySidebar | L1987 | ~25 lines |
| renderApprovals | L2144 | ~33 lines |
| renderAgents | L2383 | ~400 lines |
| renderTeams | L2785 | ~47 lines |
| renderTeamRunDetail | L2832 | ~178 lines |
| renderStatus | L3044 | ~158 lines |
| renderCanvasPanel | L3267 | ~142 lines |
| renderChat | L3410 | ~145 lines |
| renderMissionControl | L3556 | ~17 lines |
| renderProjectsBoard | L3573 | ~45 lines |
| renderProjectDetail | L3619 | ~565 lines |
| renderCode | L4185 | ~3259 lines |
| renderReposPanel | L7445 | ~239 lines |
| renderChainSummary | L8591 | ~106 lines |
| renderRoutingPanel | L8698 | ~47 lines |

**renderMarkdown (L3203, ~63 lines):** Before deleting, verify no KEPT render functions call it:
```bash
grep -n "renderMarkdown" dashboard/frontend/dist/app.js
```
If only deleted functions call it, delete it. If renderApps, renderTools, renderSkills, renderReports, or renderSettings call it, KEEP it.

**Load functions to delete** (L694-1000 range):
- `loadTasks()`
- `loadRewardsStatus()`, `toggleRewardsSystem()`
- `loadRepos()`, `loadGithubAccounts()`, `loadRepoBranches()`
- `loadApprovals()`
- `loadStatus()`
- `loadProfiles()`, `loadPersona()`
- `loadChannels()` — KEEP if Channels is a kept feature (see research Open Question 4 — `/api/channels` is not in REMOVE list, so KEEP `loadChannels()`)

KEEP these load functions:
- `loadApps()` — loads Agent Apps (D-24)
- `loadTools()`, `loadSkills()` — intelligence layer
- `loadReports()` — intelligence layer
- `loadSettings()` — intelligence layer
- `loadMemory()` (if exists) — intelligence layer
- `loadEffectiveness()` (if exists) — intelligence layer

**WebSocket message handlers** (~L580-640 area): Remove handlers for `project_updated`, `chat_message`, `chat_message_update`, and harness-specific task/agent event types. Keep handlers for intelligence layer events (memory, tool, effectiveness, provider).

After all deletions, verify the file still has valid JavaScript structure by checking that all remaining function definitions have matching braces.
  </action>
  <verify>
    <automated>cd C:/Users/rickw/projects/agent42 && grep -cE "(renderMissionControl|renderStatus|renderApprovals|renderCode|renderAgents|renderTeams|renderChat|renderKanbanBoard|renderProjectDetail|renderReposPanel|renderRoutingPanel|renderChainSummary)" dashboard/frontend/dist/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "renderMissionControl" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "renderStatus" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "renderApprovals" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "renderCode" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "renderAgents" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "renderTeams" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "renderChat" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "renderKanbanBoard" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "renderProjectDetail" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "renderReposPanel" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "renderRoutingPanel" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "renderChainSummary" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "loadTasks" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "loadApprovals" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "loadRewardsStatus" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "loadProfiles" dashboard/frontend/dist/app.js` returns 0
    - Kept functions still present: `grep -c "renderApps" dashboard/frontend/dist/app.js` returns > 0
    - Kept functions still present: `grep -c "renderTools" dashboard/frontend/dist/app.js` returns > 0
    - Kept functions still present: `grep -c "renderSkills" dashboard/frontend/dist/app.js` returns > 0
    - Kept functions still present: `grep -c "renderReports" dashboard/frontend/dist/app.js` returns > 0
    - Kept functions still present: `grep -c "renderSettings" dashboard/frontend/dist/app.js` returns > 0
  </acceptance_criteria>
  <done>All harness render functions and load functions removed from app.js. All kept intelligence layer render/load functions remain intact. ~3,000+ lines removed.</done>
</task>

<task type="auto">
  <name>Task 2: Update sidebar, renderers map, state object, and default page</name>
  <files>dashboard/frontend/dist/app.js</files>
  <read_first>
    - dashboard/frontend/dist/app.js (post-Task 1 state — re-read sidebar area, renderers map, state object)
    - .planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-RESEARCH.md (sidebar at lines 222-226, renderers at lines 227, state fields at lines 170-183, settings tabs at lines 231)
  </read_first>
  <action>
1. **Update sidebar navigation** (originally ~L8786-8796, per D-29):
   Delete the `approvalBadge` computation line (~L8778: `const approvalBadge = ...`) — per Pitfall 6.
   Replace sidebar links to show ONLY kept pages:
   ```javascript
   <a href="#" data-page="apps" ...>&#128640; Sandboxed Apps</a>
   <a href="#" data-page="tools" ...>&#128295; Tools</a>
   <a href="#" data-page="skills" ...>&#9889; Skills</a>
   <a href="#" data-page="reports" ...>&#128202; Reports</a>
   <a href="#" data-page="settings" ...>&#9881; Settings</a>
   ```
   Remove ALL of: tasks (Mission Control), status, approvals, workspace, agents, teams links.
   Remove `${state.standaloneMode ? "" : ...}` conditional wrappers — `standaloneMode` field is being removed.

2. **Update renderers map** (originally ~L8829-8843, per D-30):
   Replace the full renderers object with:
   ```javascript
   const renderers = {
     apps: renderApps,
     tools: renderTools,
     skills: renderSkills,
     reports: renderReports,
     settings: renderSettings,
   };
   ```
   Remove: tasks, status, approvals, workspace, agents, teams, detail, projectDetail entries.

3. **Update default page** (per research line 382):
   Change `state.page` initialization from `"tasks"` to `"apps"`. This is near L11 in the state object definition:
   ```javascript
   page: "apps",
   ```

4. **Remove harness state fields** from the state object (near L7-120):
   Delete these fields:
   ```
   approvals, selectedTask, viewMode, activityFeed, activityOpen, filterPriority,
   filterType, status, profiles, defaultProfile, selectedProfile, agentsViewMode,
   personaCustom, personaDefault, routingModels, routingConfig, routingEdits,
   routingSaving, agentRoutingEdits, agentRoutingSaving, selectedProfileRouting,
   chatMessages, chatInput, chatSending, canvasOpen, canvasContent, canvasTitle,
   canvasLang, chatSessions, currentSessionId, currentSessionMessages,
   codeSessions, codeCurrentSessionId, codeCurrentMessages, codeSetupStep,
   codeSending, codeCanvasOpen, panelTab, projects, selectedProject,
   missionControlTab, projectViewMode, githubConnected, githubDeviceCode,
   githubPolling, githubAccounts, githubAccountsLoading, githubAccountAdding,
   repos, repoBranches, githubRepos, githubLoading, teamRuns, selectedTeamRun,
   agents, rewardsStatus, standaloneMode
   ```
   KEEP state fields used by remaining pages: apps, tools, skills, reports, settings, memory-related fields, effectiveness-related fields, provider-related fields.

5. **Clean Settings panel tabs** (originally ~L7934):
   Remove `repos`, `channels`, `rewards` tabs from the settings tabs array IF those tab render sections reference removed features. Keep the tab if it renders intelligence layer settings.
   Remove any `!state.standaloneMode` conditionals since `standaloneMode` is deleted.

6. After all edits, verify the file parses as valid JavaScript by checking for syntax errors (open the file in the browser or use node --check if available).
  </action>
  <verify>
    <automated>cd C:/Users/rickw/projects/agent42 && grep -c "data-page=\"tasks\"" dashboard/frontend/dist/app.js && grep -c "data-page=\"agents\"" dashboard/frontend/dist/app.js && grep -c "data-page=\"teams\"" dashboard/frontend/dist/app.js && grep -c "data-page=\"status\"" dashboard/frontend/dist/app.js && grep -c "data-page=\"approvals\"" dashboard/frontend/dist/app.js && grep -c "data-page=\"workspace\"" dashboard/frontend/dist/app.js && echo "--- kept pages ---" && grep -c "data-page=\"apps\"" dashboard/frontend/dist/app.js && grep -c "data-page=\"tools\"" dashboard/frontend/dist/app.js && grep -c "data-page=\"settings\"" dashboard/frontend/dist/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-page="tasks"' dashboard/frontend/dist/app.js` returns 0
    - `grep -c 'data-page="agents"' dashboard/frontend/dist/app.js` returns 0
    - `grep -c 'data-page="teams"' dashboard/frontend/dist/app.js` returns 0
    - `grep -c 'data-page="status"' dashboard/frontend/dist/app.js` returns 0
    - `grep -c 'data-page="approvals"' dashboard/frontend/dist/app.js` returns 0
    - `grep -c 'data-page="workspace"' dashboard/frontend/dist/app.js` returns 0
    - `grep -c 'data-page="apps"' dashboard/frontend/dist/app.js` returns > 0
    - `grep -c 'data-page="tools"' dashboard/frontend/dist/app.js` returns > 0
    - `grep -c 'data-page="settings"' dashboard/frontend/dist/app.js` returns > 0
    - `grep -c "approvalBadge" dashboard/frontend/dist/app.js` returns 0
    - `grep -c "standaloneMode" dashboard/frontend/dist/app.js` returns 0
    - `grep "page:" dashboard/frontend/dist/app.js | head -1` contains "apps" (default page)
    - `grep -c "renderMissionControl" dashboard/frontend/dist/app.js` returns 0 (not in renderers map)
    - `wc -l dashboard/frontend/dist/app.js` shows reduction of ~3,000+ lines from 8,924 baseline
  </acceptance_criteria>
  <done>Sidebar shows only Apps, Tools, Skills, Reports, Settings. Renderers map contains only kept entries. Default page is "apps". All harness state fields removed. standaloneMode conditionals removed. approvalBadge computation removed.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -cE "(renderMissionControl|renderAgents|renderTeams|renderChat|renderCode|renderApprovals|renderStatus)" dashboard/frontend/dist/app.js` returns 0
2. `grep -c "standaloneMode" dashboard/frontend/dist/app.js` returns 0
3. `grep -c "approvalBadge" dashboard/frontend/dist/app.js` returns 0
4. All kept render functions present: renderApps, renderTools, renderSkills, renderReports, renderSettings
5. `wc -l dashboard/frontend/dist/app.js` shows ~5,000-6,000 lines (down from 8,924)
</verification>

<success_criteria>
- Zero harness render functions remain in app.js
- Sidebar contains exactly 5 entries: Apps, Tools, Skills, Reports, Settings
- Renderers map contains exactly 5 entries matching sidebar
- Default page is "apps"
- No harness state fields remain
- File size reduced by ~3,000+ lines
</success_criteria>

<output>
After completion, create `.planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-02-SUMMARY.md`
</output>
