---
phase: 50-strip-harness-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/server.py
autonomous: true
requirements:
  - STRIP-01
  - STRIP-02
  - STRIP-03
  - STRIP-04
  - STRIP-05
  - STRIP-06
  - STRIP-07
  - STRIP-08
  - STRIP-09
  - STRIP-10
  - STRIP-11
  - STRIP-12
  - CLEAN-01

must_haves:
  truths:
    - "No Mission Control, Workspace, Agents, Teams, Approvals, Chat, Status, GitHub, Device, GSD, Profile, Persona, Rewards, or Projects routes exist in server.py"
    - "All kept routes (auth, memory, tools, skills, reports, effectiveness, providers, llm proxy, settings, apps, health, setup) remain functional"
    - "create_app() signature has no harness parameters"
    - "No orphaned Pydantic models or enum stubs remain"
    - "No standalone_guard decorator definition or usage remains"
    - "_DASHBOARD_EDITABLE_SETTINGS has no entries for removed features"
  artifacts:
    - path: "dashboard/server.py"
      provides: "Stripped server with only intelligence layer routes"
      contains: "@app.get.*api/memory"
  key_links:
    - from: "dashboard/server.py"
      to: "agent42.py"
      via: "create_app() call"
      pattern: "create_app\\("
---

<objective>
Strip all harness route groups from dashboard/server.py, leaving only intelligence layer routes.

Purpose: This is the core deletion task of Phase 50 — removing ~1,600 lines of harness route handlers, orphaned models, stale imports, and harness-specific infrastructure from the server.

Output: A server.py with only kept routes (auth, memory, tools, skills, reports, effectiveness, providers, LLM proxy, settings, apps, health, setup, WebSocket infrastructure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frood-dashboard/ROADMAP.md
@.planning/workstreams/frood-dashboard/REQUIREMENTS.md
@.planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-CONTEXT.md
@.planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove all harness route groups from server.py (bottom-to-top)</name>
  <files>dashboard/server.py</files>
  <read_first>
    - dashboard/server.py (full file — understand current route layout before cutting)
    - .planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-RESEARCH.md (deletion order and line ranges)
    - .planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-CONTEXT.md (locked decisions D-01 through D-14, D-33)
  </read_first>
  <action>
Per D-33, remove route groups from server.py working BOTTOM-TO-TOP to avoid line number drift. Re-verify line numbers with grep before each cut (research line numbers are approximate).

Delete these route groups in this order:

1. GitHub + Repos (~L5914-6193): `/api/github/*`, `/api/repos/*` — per D-06
2. Projects (~L5727-5890): `/api/projects/*` (inside `if project_manager:` block) — per D-14
3. Devices (~L5067-5139): `/api/devices/*` — per D-08
4. Approvals (~L5049-5066): `/api/approvals` — per D-05
5. Rewards (~L4944-5048): `/api/rewards/*` — per D-13
6. Agents (~L4732-4943): `/api/agents/*` — per D-03
7. Chat + IDE Chat (~L3282-4112): `/api/chat/*`, `/api/ide/chat*` — per D-07
8. CC Sessions (~L3082-3119): `/api/cc/*` — per D-07
9. CC Chat WebSocket (~L2065-3082): `/ws/cc-chat` — per D-07
10. Terminal WebSocket (~L1776-2064): `/ws/terminal` — per D-02
11. GSD Workstreams (~L1612-1708): `/api/gsd/*` — per D-09
12. IDE routes (~L1429-1710): `/api/ide/*` — per D-02 (overlaps with GSD range — adjust boundaries by grepping for actual route decorators)
13. Workspaces (~L1376-1428): `/api/workspaces/*` — per D-02
14. Activity Feed (~L1075-1095): `/api/activity`, `_record_activity()`, `_activity_feed` list — per D-35
15. Profiles + Routing + Persona (~L858-1073): `/api/profiles/*`, `/api/agent-routing/*`, `/api/persona` — per D-11, D-12
16. Status (~L608-642): `/api/status` — per D-10

KEEP these routes untouched:
- Apps (~L6194-6340): `/api/apps/*` — per D-24
- WebSocket /ws (~L5661-5752) — per D-26 (but remove device auth branch inside it — that's Task 2 territory in Plan 03)
- Settings (~L5312-5651): `/api/settings/*` — per D-23
- Tools + Skills (~L5249-5310): `/api/tools/*`, `/api/skills/*` — per D-17, D-18
- Provider Status (~L5140-5248): `/api/providers/*` — per D-21
- Effectiveness (~L4112-4731): `/api/effectiveness/*`, `/api/learnings/*` — per D-20
- Memory (~L3168-3269): `/api/memory/*` — per D-16
- LLM Proxy (~L3641-3912): `/llm/*` — per D-22
- Reports (~L1140-1365): `/api/reports` — per D-19
- Auth (~L749-857): `/api/login`, `/api/logout` — per D-15
- Health + Setup (~L580-750): `/health`, `/api/setup/*` — per D-25

After each group removal, do a quick `grep -c` to verify the route pattern is gone from the file. Run `python -m pytest tests/ -x -q --tb=short` after removing every 3-4 groups to catch breakage early.

IMPORTANT: When removing route groups, also remove any local helper functions defined within those groups (e.g., helper functions nested inside the `create_app()` body that are only called by removed routes).
  </action>
  <verify>
    <automated>cd C:/Users/rickw/projects/agent42 && grep -cE "(api/agents|api/approvals|api/chat|api/cc|api/devices|api/gsd|api/github|api/repos|api/projects|api/profiles|api/agent-routing|api/persona|api/rewards|api/status|api/ide|api/workspaces|ws/cc-chat|ws/terminal|api/activity)" dashboard/server.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "api/agents" dashboard/server.py` returns 0
    - `grep -c "api/approvals" dashboard/server.py` returns 0
    - `grep -c "api/chat" dashboard/server.py` returns 0
    - `grep -c "api/cc/" dashboard/server.py` returns 0
    - `grep -c "api/devices" dashboard/server.py` returns 0
    - `grep -c "api/gsd" dashboard/server.py` returns 0
    - `grep -c "api/github" dashboard/server.py` returns 0
    - `grep -c "api/repos" dashboard/server.py` returns 0
    - `grep -c "api/projects" dashboard/server.py` returns 0
    - `grep -c "api/profiles" dashboard/server.py` returns 0
    - `grep -c "api/persona" dashboard/server.py` returns 0
    - `grep -c "api/rewards" dashboard/server.py` returns 0
    - `grep -c "api/ide" dashboard/server.py` returns 0
    - `grep -c "api/workspaces" dashboard/server.py` returns 0
    - `grep -c "ws/cc-chat" dashboard/server.py` returns 0
    - `grep -c "ws/terminal" dashboard/server.py` returns 0
    - `grep -c "_record_activity" dashboard/server.py` returns 0
    - `grep -c "api/activity" dashboard/server.py` returns 0
    - `grep -c "api/status" dashboard/server.py` returns 0 (note: /api/health stays, /api/status goes)
    - Kept routes still present: `grep -c "api/memory" dashboard/server.py` returns > 0
    - Kept routes still present: `grep -c "api/tools" dashboard/server.py` returns > 0
    - Kept routes still present: `grep -c "api/apps" dashboard/server.py` returns > 0
    - Kept routes still present: `grep -c "api/effectiveness" dashboard/server.py` returns > 0
    - Kept routes still present: `grep -c "api/providers" dashboard/server.py` returns > 0
  </acceptance_criteria>
  <done>All 16 harness route groups removed from server.py. All kept intelligence layer routes remain intact. No route handler for any STRIP-01 through STRIP-12 feature exists.</done>
</task>

<task type="auto">
  <name>Task 2: Clean orphaned models, imports, create_app params, and standalone_guard</name>
  <files>dashboard/server.py</files>
  <read_first>
    - dashboard/server.py (post-Task 1 state — re-read to see what remains)
    - .planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-RESEARCH.md (orphaned items list at lines 124-131)
  </read_first>
  <action>
After Task 1 removed route handlers, clean up the infrastructure debris in server.py:

1. **Remove orphaned Pydantic model classes** (~L187-290 original): Delete `TaskCreateRequest`, `InterventionRequest`, `DeviceRegisterRequest`, `ChatMessageRequest`, `ProjectCreateRequest`, and any other request/response models whose routes were removed. Verify each model name has zero references in remaining code before deleting.

2. **Remove orphaned enum stubs** (~L60-97 original): Delete `TaskStatus`, `TaskType`, `infer_task_type()` and any other harness-specific enums/functions near the top of `create_app()`. Verify zero references before deleting.

3. **Remove top-level imports** that are now unused:
   - `from core.approval_gate import ApprovalGate`
   - `from core.device_auth import DeviceStore`
   - `from dashboard.auth import API_KEY_PREFIX`
   - Any other imports flagged by `ruff check dashboard/server.py --select F401`

4. **Clean `create_app()` signature** — remove these parameters (per D-34):
   - `approval_gate`
   - `device_store`
   - `channel_manager`
   - `repo_manager`
   - `project_manager`
   - `profile_loader`
   - `github_account_store`
   - `agent_manager`
   - `reward_system`
   - `workspace_registry`
   - `standalone`
   Keep parameters needed by remaining routes (e.g., `settings`, `ws_manager`, `tool_registry`, `skill_loader`, `memory_store`, `effectiveness_tracker`, `provider_status`, `llm_router`, `key_vault`).

5. **Remove `standalone_guard`** — delete the decorator definition and grep for any `@standalone_guard` usage on remaining routes. Since all harness routes are gone, no kept routes should have this decorator. If any do, simply remove the decorator (not the route).

6. **Remove `ws_manager.record_activity = _record_activity`** (~L6453 original) — per D-35.

7. **Clean `_DASHBOARD_EDITABLE_SETTINGS`** — remove these entries:
   - `PROJECT_MEMORY_ENABLED`
   - `AGENT_DEFAULT_PROFILE`
   - `RLM_ENABLED`, `RLM_THRESHOLD_TOKENS`, `RLM_ENVIRONMENT`, `RLM_MAX_DEPTH`, `RLM_MAX_ITERATIONS`, `RLM_VERBOSE`, `RLM_COST_LIMIT`, `RLM_TIMEOUT_SECONDS`, `RLM_LOG_DIR`
   - `DEFAULT_REPO_PATH`
   - `TASKS_JSON_PATH`

8. Run `ruff check dashboard/server.py --select F401,F841` to catch any remaining unused imports or variables. Fix all findings.
  </action>
  <verify>
    <automated>cd C:/Users/rickw/projects/agent42 && ruff check dashboard/server.py --select F401,F841 && python -m pytest tests/test_auth_flow.py tests/test_llm_proxy.py -x -q --tb=short 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "standalone_guard" dashboard/server.py` returns 0
    - `grep -c "TaskCreateRequest" dashboard/server.py` returns 0
    - `grep -c "DeviceRegisterRequest" dashboard/server.py` returns 0
    - `grep -c "InterventionRequest" dashboard/server.py` returns 0
    - `grep -c "TaskStatus" dashboard/server.py` returns 0
    - `grep -c "infer_task_type" dashboard/server.py` returns 0
    - `grep -c "_record_activity" dashboard/server.py` returns 0
    - `grep -c "PROJECT_MEMORY_ENABLED" dashboard/server.py` returns 0
    - `grep -c "AGENT_DEFAULT_PROFILE" dashboard/server.py` returns 0
    - `grep -c "TASKS_JSON_PATH" dashboard/server.py` returns 0
    - `grep -c "approval_gate" dashboard/server.py` returns 0 (parameter removed from create_app)
    - `grep -c "device_store" dashboard/server.py` returns 0 (parameter removed from create_app)
    - `ruff check dashboard/server.py --select F401,F841` exits 0 (no unused imports or variables)
    - `python -m pytest tests/test_auth_flow.py -x -q` exits 0 (app still starts and auth works)
  </acceptance_criteria>
  <done>server.py is clean: no orphaned models, no unused imports, no harness parameters in create_app(), no standalone_guard, no stale settings entries. Ruff lint passes. Auth smoke test passes.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -cE "(api/agents|api/approvals|api/chat|api/devices|api/github|api/repos|api/projects|api/profiles|api/persona|api/rewards|api/status|api/ide|api/workspaces)" dashboard/server.py` returns 0
2. `grep -c "standalone_guard" dashboard/server.py` returns 0
3. `ruff check dashboard/server.py --select F401,F841` exits 0
4. `wc -l dashboard/server.py` shows reduction of ~1,600+ lines from 6,455 baseline
5. `python -m pytest tests/test_auth_flow.py tests/test_llm_proxy.py tests/test_effectiveness.py -x -q --tb=short` passes
</verification>

<success_criteria>
- server.py contains zero route handlers for any STRIP-01 through STRIP-12 feature
- All kept routes (auth, memory, tools, skills, reports, effectiveness, providers, LLM proxy, settings, apps, health, setup) remain present
- No orphaned Pydantic models, unused imports, or harness parameters in create_app()
- ruff lint passes with no F401/F841 findings
- Auth and LLM proxy smoke tests pass
</success_criteria>

<output>
After completion, create `.planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-01-SUMMARY.md`
</output>
