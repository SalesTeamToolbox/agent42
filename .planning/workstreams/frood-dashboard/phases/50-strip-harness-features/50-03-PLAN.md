---
phase: 50-strip-harness-features
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/auth.py
  - dashboard/websocket_manager.py
  - agent42.py
autonomous: true
requirements:
  - STRIP-08
  - CLEAN-03

must_haves:
  truths:
    - "auth.py has no device API key validation — JWT only"
    - "agent42.py passes no harness parameters to create_app()"
    - "websocket_manager.py has no device_id or device_name fields"
    - "Application starts successfully with simplified auth and launcher"
  artifacts:
    - path: "dashboard/auth.py"
      provides: "JWT-only authentication"
      contains: "def get_auth_context"
    - path: "dashboard/websocket_manager.py"
      provides: "Simplified WebSocket manager without device fields"
      contains: "class WSConnection"
    - path: "agent42.py"
      provides: "Launcher without harness initialization"
      contains: "create_app("
  key_links:
    - from: "agent42.py"
      to: "dashboard/server.py"
      via: "create_app() call with matching params"
      pattern: "create_app\\("
    - from: "dashboard/auth.py"
      to: "dashboard/server.py"
      via: "get_auth_context dependency injection"
      pattern: "Depends.*get_auth_context"
---

<objective>
Simplify auth.py to JWT-only, clean device fields from websocket_manager.py, and update agent42.py launcher to stop passing harness parameters.

Purpose: These three files form the supporting infrastructure for the dashboard. After server.py routes are stripped (Plan 01), these files still carry dead harness code — device auth paths, harness module imports, and harness initialization in the launcher.

Output: Clean auth.py (JWT-only), clean websocket_manager.py (no device fields), clean agent42.py (no harness imports or create_app harness params).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frood-dashboard/ROADMAP.md
@.planning/workstreams/frood-dashboard/REQUIREMENTS.md
@.planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-CONTEXT.md
@.planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Simplify auth.py to JWT-only and clean websocket_manager.py</name>
  <files>dashboard/auth.py, dashboard/websocket_manager.py</files>
  <read_first>
    - dashboard/auth.py (full 218-line file — understand all device auth code paths)
    - dashboard/websocket_manager.py (full 101-line file — find device_id/device_name fields)
    - .planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-RESEARCH.md (auth.py simplification at lines 153-166, WebSocket pitfall at lines 309-312)
  </read_first>
  <action>
**auth.py changes** (per D-31, D-32):

1. Remove the import: `from core.device_auth import API_KEY_PREFIX, DeviceStore`

2. Remove module-level globals:
   - `_device_store: DeviceStore | None = None`

3. Remove functions entirely:
   - `init_device_store()` — initializes device auth store
   - `_validate_api_key()` — validates device API keys

4. Simplify `get_auth_context()` — remove the `if token.startswith(API_KEY_PREFIX):` branch.
   Before:
   ```python
   def get_auth_context(credentials = Depends(security)) -> AuthContext:
       token = credentials.credentials
       if token.startswith(API_KEY_PREFIX):
           return _validate_api_key(token)
       return _validate_jwt(token)
   ```
   After:
   ```python
   def get_auth_context(credentials = Depends(security)) -> AuthContext:
       return _validate_jwt(credentials.credentials)
   ```

5. Simplify `get_current_user_optional()` — remove the API key branch (same pattern as get_auth_context).

6. Simplify `require_admin()` — remove the API key check.

7. Clean `AuthContext` dataclass — remove `device_id` and `device_name` fields if they exist. Keep `user` and `auth_type` fields.

8. Remove `API_KEY_PREFIX` from module exports (it was imported by server.py — that import is cleaned in Plan 01).

9. Run `ruff check dashboard/auth.py --select F401,F841` and fix any remaining issues.

**websocket_manager.py changes** (per Pitfall 4):

1. Remove `device_id` and `device_name` fields from the `WSConnection` dataclass.
2. Remove the `connected_device_ids()` method (its only caller was the /api/devices route being removed in Plan 01).
3. Simplify `connect()` method to not accept `device_id`/`device_name` parameters.
4. Run `ruff check dashboard/websocket_manager.py --select F401,F841`.
  </action>
  <verify>
    <automated>cd C:/Users/rickw/projects/agent42 && grep -c "API_KEY_PREFIX" dashboard/auth.py && grep -c "_validate_api_key" dashboard/auth.py && grep -c "init_device_store" dashboard/auth.py && grep -c "_device_store" dashboard/auth.py && grep -c "device_id" dashboard/websocket_manager.py && grep -c "connected_device_ids" dashboard/websocket_manager.py && ruff check dashboard/auth.py dashboard/websocket_manager.py --select F401,F841 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "API_KEY_PREFIX" dashboard/auth.py` returns 0
    - `grep -c "_validate_api_key" dashboard/auth.py` returns 0
    - `grep -c "init_device_store" dashboard/auth.py` returns 0
    - `grep -c "_device_store" dashboard/auth.py` returns 0
    - `grep -c "DeviceStore" dashboard/auth.py` returns 0
    - `grep -c "device_id" dashboard/websocket_manager.py` returns 0
    - `grep -c "device_name" dashboard/websocket_manager.py` returns 0
    - `grep -c "connected_device_ids" dashboard/websocket_manager.py` returns 0
    - `ruff check dashboard/auth.py --select F401` exits 0
    - `ruff check dashboard/websocket_manager.py --select F401` exits 0
    - `grep -c "get_auth_context" dashboard/auth.py` returns > 0 (function still exists, just simplified)
    - `grep -c "_validate_jwt" dashboard/auth.py` returns > 0 (JWT validation still exists)
  </acceptance_criteria>
  <done>auth.py is JWT-only with no device auth code paths. websocket_manager.py has no device fields or methods. Both files pass ruff lint.</done>
</task>

<task type="auto">
  <name>Task 2: Update agent42.py launcher — remove harness imports and create_app params</name>
  <files>agent42.py</files>
  <read_first>
    - agent42.py (full 436-line file — find all harness imports and create_app call)
    - .planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-RESEARCH.md (agent42.py cleanup at lines 234-249)
    - dashboard/auth.py (post-Task 1 — verify init_device_store is gone so agent42 import must go)
  </read_first>
  <action>
Per research lines 234-249, update agent42.py to stop importing and initializing harness modules:

1. **Remove these imports:**
   - `from core.device_auth import DeviceStore`
   - `from core.project_manager import ProjectManager`
   - `from core.repo_manager import RepositoryManager`
   - `from core.workspace_registry import WorkspaceRegistry`
   - `from dashboard.auth import init_device_store`
   - Any other import of a harness-only module. Check each import against the "what to keep" list.

2. **Remove from `__init__()` method:**
   - `self.device_store` assignment and `init_device_store()` call
   - `self.repo_manager` assignment
   - `self.project_manager` assignment
   - `self.workspace_registry` assignment

3. **Remove from `start()` method:**
   - `await self.repo_manager.load()` (if present)
   - `await self.project_manager.load()` (if present)
   - `await self.workspace_registry.*` calls (if present)

4. **Remove `github_account_store` creation** (~L295-298 original)

5. **Update `create_app()` call** (~L299-316 original) — remove these keyword arguments:
   - `approval_gate=...`
   - `device_store=...`
   - `channel_manager=...`
   - `repo_manager=...`
   - `project_manager=...`
   - `profile_loader=...`
   - `github_account_store=...`
   - `workspace_registry=...`
   - `standalone=...`

   KEEP these (used by remaining routes):
   - `agent_manager=self.agent_manager` — used by sidecar.py and tiered_routing_bridge.py
   - `reward_system=self.reward_system` — used by sidecar.py and tiered_routing_bridge.py
   - `settings=...`, `ws_manager=...`, `tool_registry=...`, `skill_loader=...`
   - `memory_store=...`, `effectiveness_tracker=...`, `provider_status=...`
   - `llm_router=...`, `key_vault=...`
   - Any other parameter for kept features

6. Run `ruff check agent42.py --select F401,F841` and fix any remaining unused imports.

IMPORTANT: Do NOT remove `self.agent_manager`, `self.reward_system`, or `self.tier_recalc` — these are used by sidecar.py and tiered_routing_bridge.py (per research anti-patterns section).
  </action>
  <verify>
    <automated>cd C:/Users/rickw/projects/agent42 && grep -c "WorkspaceRegistry" agent42.py && grep -c "ProjectManager" agent42.py && grep -c "RepositoryManager" agent42.py && grep -c "init_device_store" agent42.py && ruff check agent42.py --select F401,F841 2>&1 && python -c "import agent42; print('import OK')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WorkspaceRegistry" agent42.py` returns 0
    - `grep -c "ProjectManager" agent42.py` returns 0
    - `grep -c "RepositoryManager" agent42.py` returns 0
    - `grep -c "init_device_store" agent42.py` returns 0
    - `grep -c "device_store" agent42.py` returns 0
    - `grep -c "github_account_store" agent42.py` returns 0
    - `ruff check agent42.py --select F401` exits 0
    - `grep -c "agent_manager" agent42.py` returns > 0 (kept — used by sidecar)
    - `grep -c "reward_system" agent42.py` returns > 0 (kept — used by sidecar)
    - `python -c "import agent42"` does not raise ImportError
  </acceptance_criteria>
  <done>agent42.py imports no harness modules, initializes no harness objects, and passes only kept parameters to create_app(). Ruff lint passes. Module imports successfully.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -cE "(DeviceStore|init_device_store|API_KEY_PREFIX)" dashboard/auth.py` returns 0
2. `grep -c "device_id" dashboard/websocket_manager.py` returns 0
3. `grep -cE "(WorkspaceRegistry|ProjectManager|RepositoryManager)" agent42.py` returns 0
4. `ruff check dashboard/auth.py dashboard/websocket_manager.py agent42.py --select F401,F841` exits 0
5. `python -c "import agent42"` succeeds
</verification>

<success_criteria>
- auth.py is JWT-only — no device auth code paths, no DeviceStore references
- websocket_manager.py WSConnection has no device_id/device_name fields
- agent42.py imports no harness modules and passes no harness params to create_app()
- All three files pass ruff lint
- agent42 module imports without errors
</success_criteria>

<output>
After completion, create `.planning/workstreams/frood-dashboard/phases/50-strip-harness-features/50-03-SUMMARY.md`
</output>
