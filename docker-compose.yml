version: "3.8"

# Frood development stack with optional backends
# Usage: cp .env.example .env && nano .env && docker compose up -d

services:
  frood:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DASHBOARD_HOST=0.0.0.0
      - REDIS_URL=redis://redis:6379/0
      - QDRANT_URL=http://qdrant:6333
      - QDRANT_ENABLED=true
    volumes:
      - frood-data:/app/.frood
    depends_on:
      redis:
        condition: service_started
      qdrant:
        condition: service_started
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3

  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
    volumes:
      - qdrant-data:/qdrant/storage
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6333/healthz"]
      interval: 10s
      timeout: 3s
      retries: 3

volumes:
  frood-data:
  redis-data:
  qdrant-data:
