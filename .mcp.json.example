{
  "mcpServers": {
    "frood": {
      "command": "python",
      "args": ["/path/to/frood/mcp_server.py"],
      "env": {
        "FROOD_WORKSPACE": "${workspaceFolder}",
        "QDRANT_URL": "http://localhost:6333",
        "REDIS_URL": "redis://localhost:6379/0"
      }
    },
    "_comment_remote_ssh": "Remote node via SSH stdio (simplest — uses SSH config):",
    "frood-remote": {
      "command": "ssh",
      "args": ["frood-prod", "cd ~/frood && .venv/bin/python mcp_server.py"],
      "env": {}
    },
    "_comment_remote_sse": "Remote node via SSE (requires mcp_server.py --transport sse on VPS):",
    "frood-remote-sse": {
      "type": "sse",
      "url": "http://localhost:8100/sse",
      "_note": "Use SSH tunnel: ssh -L 8100:localhost:8100 frood-prod"
    }
  }
}
